# 테스트 기간(USE_ENV_FILE=True)에만 사용하는 파일입니다.
# 이 파일을 복사해서 이름을 ".env"로 바꾸고, 아래 값을 실제 KRX 계정으로 채우세요.
#
#   cp .env.example .env
#
# .env 파일은 절대 GitHub 등에 올리지 마세요 (비밀번호 노출 위험).

KRX_ID=본인_KRX_아이디
KRX_PW=본인_KRX_비밀번호
